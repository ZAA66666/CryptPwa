# CryptPwa 使用教程

本教程按主页功能逐项讲解。所有工具的操作逻辑基本一致：**输入内容 → 设置参数 → 点击执行 → 查看/复制结果**。

---

## 目录

1. [哈希](#1-哈希)
2. [编解码](#2-编解码)
3. [加/解密（对称 + 非对称）](#3-加解密)
4. [二维码 / 条形码](#4-二维码--条形码)
5. [JSON 工具](#5-json-工具)
6. [Crontab 定时表达式](#6-crontab-定时表达式)
7. [随机文本](#7-随机文本)
8. [密码本（常用密码管理）](#8-密码本)
9. [设置](#9-设置)
10. [外部调用与分享](#10-外部调用与分享)

---

## 1. 哈希

**用途**：把任意文本变成固定长度的"指纹"，用于校验文件完整性、验证口令等。哈希是**单向**的，无法还原原文。

- 输入文本 → 选择算法（MD5 / SHA1 / SHA256 / SHA512…）→ 点「计算」
- **HMAC**（带密钥的哈希）：选 HMAC 系列算法并填写密钥，计算后可以「保存到密码本」（只有 HMAC 会提示保存，普通哈希不会）

## 2. 编解码

**用途**：格式转换，如 Base64、Hex、URL 编码、JWT 解析等。编码不是加密，可逆且不保密。

- 选择「编码」或「解码」→ 选择方式（Base64 / Hex / URL / Base32 / Base58 / Unicode / JWT）→ 输入内容 → 点「编码/解码」或「执行」
- **切换方式会自动按上次操作重新计算**（比如先编码成 Base64，再切到 Hex，结果会自动更新）
- 执行成功/失败会有提示；图片转 Base64：选择图片文件 → 点「图片 → Base64」

## 3. 加/解密

主页「加/解密」进入，顶部切换 **对称加密 / 非对称加密**。

### 3.1 对称加密（AES / DES / 3DES / Blowfish / RC4 / Rabbit）

- **算法**：AES 支持 128/192/256 位密钥长度（下方「密钥长度」选择）；其余按提示填写
- **分组模式**：AES/DES/3DES/Blowfish 可选 ECB / CBC / CTR / CFB / OFB（ECB 不需要 IV，其余需填写 **IV**，长度与算法块大小一致，通常 16 字节）
- **随机生成**：密钥/IV 旁边有随机按钮，一键生成
- 加密后点「保存到密码本」，**密钥 + 模式 + 密钥长度 + IV 会一起保存**；下次从密码本填入，所有参数自动恢复，直接解密即可
- ⚠️ **重要**：CBC 等模式的密文现已**自动携带 IV**（`v1:...` 格式），解密时自动提取，无需手填；老格式密文若未保存 IV 则无法解密

### 3.2 非对称加密（RSA / SM2）

- **RSA**：进入面板自动生成密钥对（2048 位，存本地）。用「查看/修改密钥对」查看、复制、**粘贴导入**（自动校验 PEM 格式）、重新生成、保存到密码本
- **SM2（国密）**：同样自动生成密钥对；加密用公钥、解密用私钥；支持签名/验签
- 从密码本选择密钥后，顶部状态会显示「已从密码本选择：xxx」
- 私钥请妥善保管（可导出到文件），泄露等于丢失安全性

## 4. 二维码 / 条形码

- **生成**：输入内容（文本/网址）→ 选容错级别 → 生成；条形码同页面生成
- **扫描**：点「扫描二维码」→ 授权摄像头对准识别；或「从相册选择」图片识别
- 识别结果可复制，也能「填入」到其他工具继续处理

## 5. JSON 工具

三个子标签：

- **格式化**：粘贴 JSON → 格式化（缩进）/ 压缩 / 校验，底部有校验提示
- **提取代码**：按路径提取，自动生成对应代码片段（如取值、遍历）
- **键值编辑**：逐行添加键/值/类型（字符串/数字/布尔/空）→「生成 JSON」；「＋ 模板」可一键填入随机示例或 JSON 模板；「从格式化区导入」把上方 JSON 转为键值行

## 6. Crontab 定时表达式

输入 Cron 表达式（5 段或 6 段，支持 `*`、列表、区间、步长）→ 解析出**最近 5 次执行时间**，直观验证表达式是否正确。

## 7. 随机文本

生成随机密码/数字/字符串：可配置长度、字符集（大小写/数字/符号）、数量，一键复制。

## 8. 密码本

管理常用口令、密钥、RSA/SM2 密钥对的地方（设置 → 密码本）。

### 首次使用
设置一个**主密码**（AES-256-CBC 加密存储，忘记无法找回，请务必牢记）。

### 解锁
输入主密码解锁后即可看到已保存列表；**每次打开 App 只需解锁一次**。

### 3 套密码库
密码本支持 **库 1 / 库 2 / 库 3** 三套独立密码库：

- 页面顶部切换库号，**展示的就是该库保存的密码**；新保存的条目归属当前库
- 长按页面顶部的「返回」按钮会弹出使用提示

### 保存/填入
- 各功能执行后（哈希 HMAC、加解密等）选择保存，自动带出算法、密钥、IV 等信息
- 面板上的「密码本」按钮可从密码本选择填入（自动恢复算法/模式/密钥长度/IV）

### 导出 / 导入（CryptoData.json）
- 导出时弹窗选择 **🔒 加密保存** 或 **📄 明文保存**；文件格式 `{Version, UpdateTime, UserData}`
- 加密文件导入需输入主密码解密；明文文件直接导入；旧版备份包兼容

## 9. 设置

| 分组 | 功能 |
|---|---|
| **通用设置** | 语言（中文/英文/导入语言包）、主题（亮/暗/跟随系统）、强调色、字体大小、沉浸式、外部调用与分享、**实验性**（数据回调/导入方式）、内容保存路径 |
| **数据隐私** | **数据加密开关**（默认开启：密码本密文存储；关闭后明文存储无需主密码）、密码本、数据备份与同步（WebDAV） |
| **隐私与条款** | 关于（版本/检测更新/项目源码）、隐私政策、用户协议、安全说明、个人信息 |

- **导入语言包**：选择 JSON 文件（`{lang, name, data}`），可加载自翻译语言
- **数据加密开关**：切换时自动迁移数据（关→开需设主密码，开→关需先解锁）
- **WebDAV**：配置服务器后可备份/恢复（支持坚果云、Nextcloud、群晖等），配好后可点「检测连接」验证（绿=通，红=不通）
- **关于 → 检测更新**：对比 GitHub Releases 版本并跳转下载

## 10. 外部调用与分享

- **自动弹出**：开关开启时，通过 `crypto-pwa://?text=...` 或系统分享传入文本/JSON/图片，App 自动弹出选择器处理
- **导入方式**（实验性）：URL Scheme、Android Intent、系统分享、剪贴板四种传入方式说明
- **数据回调**（实验性）：处理完成后生成回调数据 `{ok, ts, data}` + 回调地址示例（URL Scheme / Android Intent），供调用方接收；纯网页受浏览器限制，打包安卓 App 后可实现真正自动回调

---

## 常见问题

**Q：网页版能加密后发给别人解密吗？**
能。对称加密需把「算法 + 密钥 + IV + 密文」一起给对方（CBC 密文已含 IV，只需算法 + 密钥）；非对称加密用对方公钥加密、对方用私钥解密。

**Q：忘记主密码怎么办？**
无法恢复（本地无明文存储）。请务必记住主密码，或定期用「导出备份」加密备份。

**Q：安卓 APK 在哪下载？**
仓库 Releases 页面，每次推送代码自动构建。安装时允许「未知来源」即可（debug 签名，仅个人使用）。

# 🤖 AI 生成声明 / AI Disclosure

**中文**

本项目（CryptPwa）的**代码由 AI 辅助生成（AI-generated code）**，包括但不限于：

- 页面结构（HTML）、样式（CSS）与脚本逻辑（JavaScript）
- 功能实现（哈希 / 编解码 / 加解密 / 二维码 / JSON / Crontab / 随机 / 密码本 / 设置 / 备份同步 / 外部调用等）
- 本项目 README 与教程文档

**使用提示**

- 本项目仅供**学习交流与个人使用**，不构成任何商业产品或安全承诺。
- 使用前请**自行审查代码安全性**（尤其是加密实现、密码本存储等敏感部分）。
- 密码本数据加密采用 AES-256-CBC + PBKDF2（10,000 次），密钥派生在主密码基础上完成；请妥善保管主密码，丢失无法恢复。
- 如将本项目用于生产环境或处理敏感数据，强烈建议由专业人员审计后使用。

---

**English**

This project (CryptPwa) contains **AI-generated code**, including but not limited to:

- HTML structure, CSS styles, and JavaScript logic
- All features (hashing / encode-decode / encryption / QR / JSON / Crontab / random / password book / settings / backup & sync / external invocation, etc.)
- The README and tutorial documents in this repository

**Usage notice**

- This project is for **learning and personal use only**; it provides no commercial warranty or security guarantee.
- **Review the code for security yourself** before use (especially the crypto implementation and password book storage).
- The password book is encrypted with AES-256-CBC + PBKDF2 (10,000 iterations); keep your master password safe — it cannot be recovered if lost.
- For production or sensitive data, have the code audited by professionals first.

name: Keystore Inline Test (secret referenced inline, no env block)

on:
  push:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - run: echo "before"
      - name: inline secret reference
        run: |
          VAL="${{ secrets.CRYPTPWA_P12 }}"
          echo "got value length ${#VAL}"
      - run: echo "after"

# Crypto-pwa 代码结构说明

一个完全本地运行的加解密工具箱（PWA），用于学习与实践加解密、编码、二维码、国密 SM2 等操作。作者：ZAA66666，仓库：https://github.com/ZAA66666/Crypto-pwa

## 目录总览

```
crypto-pwa/
├── index.html              页面骨架：主页卡片、全部功能面板、弹窗浮层
├── manifest.webmanifest    中文 PWA 清单（APP 名/图标/分享目标）
├── manifest-en.webmanifest 英文 PWA 清单（切换语言时按需加载）
├── sw.js                   Service Worker：离线缓存 + 版本号（每次改动升版本强刷）
├── icons/                  应用图标
├── css/
│   └── style.css           全部样式（主题变量、组件、弹窗、明暗配色）
└── js/
    ├── i18n.js             多语言字典（zh/en）+ 全局常量（GITHUB_REPO、APP_VERSION）+ applyLang
    ├── app.js              各功能面板逻辑（哈希/编解码/加解密/二维码/JSON/Crontab/随机文本）
    ├── settings.js         设置页：主题/语言/字体/密码本/备份同步/关于·隐私·协议
    └── vendor/             离线第三方库（crypto-js、qrcode-generator、jsQR、JsBarcode、sm-crypto、jsbn）
```

## 一、主页与面板入口（index.html + app.js）

- **主页工具网格**（`index.html` 的 `.tool-grid`）：哈希 / 编/解码 / **加/解密**（唯一入口）/ 二维码/条形码 / JSON / Crontab / 随机文本 / 教程。
- **「加/解密」面板**（`#panel-sym`，`index.html` 约 140 行）是合并后的统一入口，内部两级切换：
  1. **类别切换**（`#sym-cat` 顶部 seg）：`对称加密` / `非对称加密`
     - `对称加密` → `#sym-pane-sym`：AES/DES/3DES/Blowfish/RC4/Rabbit，密钥/模式/IV/随机密钥
     - `非对称加密` → `#sym-pane-asym`：内部再分 RSA / SM2
  2. **算法切换**（`#asym-algo`）：RSA / SM2 国密，各自独立密钥状态区 + 弹窗查看/修改
- **面板切换**（`js/app.js` `showPanel()`）：`"asym"`/`"sm2"` 都会映射到 `"sym"` 并切到对应类别，历史记录与深链 `?tab=asym` 兼容。
- 类别与算法状态：`symCat` / `asymAlgo` 全局变量（app.js 顶部），`setSymCat()` / `setAsymAlgo()` 控制显隐与密钥预生成。

## 二、备份与同步（js/settings.js）

- **备份范围选择**：`pickScope(isRestore, onOk)` 弹窗勾选「密码本 / 软件配置」（默认全勾）。
- **备份打包**：`buildBackup(opts)` 按范围打包 `{format, version, vault?, settings?}`；`applyBackup(str, scope)` 按范围恢复。
- **四个入口**（`renderSync()`）：本地导出 / 本地导入 / WebDAV 备份 / WebDAV 恢复，均先弹范围选择。
- **WebDAV 配置**：`webdavConfig()/saveWebdav()/wdAuth()/wdTarget()` + 快捷填入地址 chips。
- **密码本面板内**（`renderCommon()`）导出/导入为 **CryptoData.json**（`{Version,UpdateTime,UserData}`）：导出弹窗选「加密保存」（UserData=主密码 AES 密文）/「明文保存」（UserData=明文 JSON）；导入自动识别密文（输主密码解密）与明文。
- **数据加密开关**（设置→数据隐私）：默认开启，密码本密文存储（主密码解锁，会话内仅输 1 次）；关闭则明文存储无需主密码（`set_vault_plain`），切换时自动迁移数据。

## 三、密码本 3 套密码库（js/settings.js）

- 保险库为单份 AES 密文（`VAULT_KEY="set_vault"`），条目带 `slot` 字段（0/1/2）归属密码库；当前库号 `vaultSlot` 存 `localStorage["set_vault_slot"]`，`setVaultSlot(n)` 切换。
- `renderCommon()` 解锁分支顶部有库选择器（`#vault-slot-seg`：库 1/库 2/库 3），点击切换并重绘；列表/空提示仅展示当前库（`inSlot(p)` 过滤，删除/改名保留全量索引）。
- 保存归属：密码本面板「新增」（cp-save）与各功能「保存到密码本」（openVaultPrompt `commitNew`）都写入当前 `vaultSlot`；弹出选择列表也只显示当前库条目。
- 长按密码本页「返回」按钮（`#settings-back`）→ toast 显示 `vault.slotHint`（3 库说明）。
- 备份/导入为整库操作（含全部 3 库），不受当前库影响。

## 四、内容保存路径（设置 → 内容保存路径，js/settings.js `renderSubview("storage")`）

- 说明提示：`storage.hint`（保存行为说明）+ `storage.locHint`（本地结果数据保存位置说明）。
- 「选择文件夹」按钮（`#sp-pick`）：调 `window.showDirectoryPicker()`（Chrome/Edge），取目录名写入 `sdcard/<目录名>`；不支持时提示手动输入。
- 「恢复默认」按钮（`#sp-reset`）：一键恢复 `sdcard/CryptoPwa`。
- 保存值：`setSetting("savepath", ...)`，实际写文件走 `saveTextFile()`（app.js，优先 File System Access，否则下载）。

## 五、检测更新（设置 → 关于，js/settings.js）

- 关于页底部按钮「🔄 检测更新」（`#about-check-update`）+「前往 GitHub 查看」。
- 逻辑：`fetch("https://api.github.com/repos/ZAA66666/Crypto-pwa/releases/latest")` 取最新 tag，与本地 `APP_VERSION`（`js/i18n.js` 顶部常量，当前 1.0.0）对比，有新版 → confirm 后 `window.open(rel.html_url)`；失败显示提示。
- 版本号只需改 `APP_VERSION` 一处。

## 六、多语言（js/i18n.js）

- 支持 2 种内置语言：`window.I18N = { zh, en }`（各 458 键，键完全对齐）；`t(key)` 取当前语言文案；`applyLang()` 切换 `document.title`、`<html lang/dir>`、PWA manifest（`manifest.webmanifest` / `manifest-en.webmanifest`）、所有 `data-i18n` 文本与 `data-i18n-attr` 属性。
- 语言检测：`resolveLang()`（settings.js）跟随系统时 `navigator.language` 以 zh 开头 → 中文，其余一律英文（非中文默认英文）。
- **语言包导入**（设置 → 语言「📦 导入语言包」）：选择 JSON（`{lang, name, data}`，data 为键值翻译对象）→ 校验（lang 为 2-5 位小写字母、含 `appTitle`/`tool.hash` 等关键键）→ 注册 `window.I18N[lang]` + 持久化 `localStorage["set_imported_langs"]` → 语言选择器动态列出该语言并自动切换。翻译底稿：根目录 `中文语言包_zh.json`（458 键 zh 全量，`{lang:"zh", name:"简体中文", data:{...}}` 格式，可直接翻译后导入）。
- `LANG_META` 常量映射语言 → html lang / dir / manifest（zh/en 均为 LTR；RTL 支持代码保留，将来加回阿拉伯语可直接复用）。
- 语言选择：设置 → 显示设置 / 语言（`lang-seg` 含 system/zh/en + 已导入语言）；`resolveLang()` 跟随系统时 `navigator.language` 以 zh 开头 → 中文，其余 → 英文。
- 曾内置 ja/ko/ar 三本字典（含 RTL 支持），为减小体积已移除；如需再支持其它语言，直接翻译根目录 `中文语言包_zh.json` 模板，在「设置 → 语言 → 导入语言包」导入即可，无需改源码。
- 全局常量：`GITHUB_REPO`（仓库链接，关于页/检测更新共用）、`APP_VERSION`（版本号）。
- **注意**：历史上曾出现 cron/rand 的英文键误插入 zh 字典导致中文环境显示英文——本说明强调维护时保证各语言键严格对齐（可用脚本校验 `Object.keys` 一致）。

## 七、离线依赖（js/vendor/）

| 库 | 用途 | 面板 |
|---|---|---|
| crypto-js | 哈希 / 对称加密 | 哈希、加/解密（对称）、编/解码（图片转 Base64） |
| qrcode-generator | 二维码生成 | 二维码 / 条形码 |
| jsQR | 摄像头扫码 | 扫描二维码 |
| JsBarcode | 条形码生成 | 条形码 |
| sm-crypto（+jsbn） | SM2 国密 | 加/解密 → 非对称加密 → SM2 |
| Web Crypto（原生） | RSA 密钥/加解密/签名 | 加/解密 → 非对称加密 → RSA（仅 https/localhost） |

## 八、其他约定

- Service Worker：每次发版在 `sw.js` 升 `CACHE_NAME` 版本号（当前 v32），用户刷新即强刷，不被旧缓存卡住。
- 历史记录：`addHistory()` 写入 localStorage，`historyDisplayName()` 生成本地化名称，点击可跳回原功能。
- 密码本：密文存储（AES-256-CBC，PBKDF2 派生），主密码仅存内存；RSA 密钥对/单把导入有专属分类徽章；共 3 套密码库（条目 `slot` 字段），面板顶部切换库号，保存归属当前库。
